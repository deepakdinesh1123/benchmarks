# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  - main

pool: default

resources:
  pipelines:
    - pipeline: benchmarks
      source: mainrepo
      project: Benchmarks
      trigger:
        branches:
          - main

steps:
- script: conda create --name exp python=3.10 --quiet
